Drop a real app screenshot or GIF here named:

    app-screenshot.png   (or .gif)

The landing page auto-detects it: if the file exists it replaces the
animated mockup in the "See it in action" section. If it's missing,
the animated preview is shown instead. No code changes needed.

Recommended: ~1600px wide, 16:10-ish, dark background to match the page.
For a GIF, keep it short and under ~5 MB so the page loads fast.
